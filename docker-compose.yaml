version: '3.8'

# 注意：此文件已拆分为两个独立的文件：
# - docker-compose.frontend.yaml (前端服务)
# - docker-compose.backend.yaml (后端服务)
#
# 如果需要同时启动前后端，可以使用：
#   docker-compose -f docker-compose.frontend.yaml -f docker-compose.backend.yaml up
#
# 或者分别启动：
#   docker-compose -f docker-compose.frontend.yaml up -d
#   docker-compose -f docker-compose.backend.yaml up -d
